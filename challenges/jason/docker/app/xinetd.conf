service crashbash
{
    disable = no
    socket_type = stream
    protocol    = tcp
    wait        = no
    user        = he
    bind        = 0.0.0.0
    server      = /run.sh
    type        = UNLISTED
    port        = 1337
    working_dir = /tmp
}